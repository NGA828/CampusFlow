import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // The "sync state after mount" pattern (e.g. hydrating a session from
      // localStorage, or resetting loading state when a fetch effect runs) is
      // the canonical way to avoid hydration mismatches. The rule's
      // alternative (deriving everything during render) is not possible for
      // browser-only stores, so we relax it project-wide.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
