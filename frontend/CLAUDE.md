@AGENTS.md

# CampusFlow

Full specification: /PROMPT.md — read it before starting. Hard gates:
design research before UI (docs/design-research.md,
docs/design-decisions/), no generic templates, centralized API client
via NEXT_PUBLIC_API_URL, backend-authoritative state, real-time via
Laravel Echo with cleanup, loading/empty/error states everywhere,
implemented-screen score ≥ 8/10 before "complete".
